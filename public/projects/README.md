# Project assets

- Screenshots: `/projects/your-file.png` → set `screenshot` in `projects.js`
- Map pin marks: `/projects/marks/your-mark.png` → set `mark` in `projects.js`

Example mark already here: `marks/cycle.png` (for a specific project pin, not the site logo).
